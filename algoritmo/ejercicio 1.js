// Definimos los valores de a y b
const a = 8;
const b = 5;

// Calculamos v según la fórmula dada
const v = (2 * b + a / 2 + 4 * b) % a;

// Mostramos el resultado
console.log("El valor de v es: ", v);